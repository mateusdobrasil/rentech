// Primitivos de UI compartilháveis entre web/ e mobile/. Ainda vazio —
// componentes React DOM (web) e React Native (mobile) não são intercambiáveis
// por padrão; o que entrar aqui deve ser cross-platform de verdade (ex.:
// tokens de design, formatação, ou componentes React Native + react-native-web).
export {};
