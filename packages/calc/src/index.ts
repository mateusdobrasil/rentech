// Lógica de cálculo compartilhada entre web/ e mobile/ (simuladores, folha de
// pagamento, etc). Ainda vazio — a lógica hoje vive só em app/lib/ do Next.js
// (ex.: gerarHoleritePdf.ts). Migrar pra cá caso o mobile precise reusá-la,
// em vez de duplicar.
export {};
